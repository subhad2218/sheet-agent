{
  "name": "sheet-agent-desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:backend": "node scripts/build-backend.cjs",
    "tauri": "tauri",
    "start": "npm run build:backend && npm run build && tauri dev",
    "release": "npm run build:backend && npm run build && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^5.0.0"
  }
}
